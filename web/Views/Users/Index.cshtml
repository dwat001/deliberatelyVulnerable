@model IEnumerable<web.Models.Users.UserListItem>

@{
    ViewData["Title"] = "Users";
}

<h2>Users</h2>

<table class="table">

    <tr>
        <th>Username</th>
        <th>Is Admin</th>
    </tr>

    @foreach (var user in Model)
    {
        <tr>
            <td>@user.UserName</td>
            <td>@user.IsAdmin</td>
        </tr>
    }

</table>
